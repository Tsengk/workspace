# 修复提交记录

> 关联输出物模板：[输出物模板.md#10.2](../输出物模板.md)
> 目标：问题修复或规避实现，修复代码可运行
> 关联根因分析记录：[根因分析记录.md](./根因分析记录.md)
> 对应代码：auto_calibration 仓库（提交 `84cbc0c4`，FD标定，2026-06-12）

---

## 修复基本信息

| 字段 | 内容 |
|------|------|
| **覆盖范围** | 两个问题的修复实现：问题A 雷达标定 LiDAR 参数合并；问题B 相机-LiDAR 画面卡顿 |
| **结合场景** | 根因分析记录 10.1（A：`getLidarInstall` 未合并标定结果；B：时间戳同步过严致整帧跳过） |
| **完成标准** | 修复代码可运行 |
| **状态** | [x] 已完成（修复编译运行正常，当前 HEAD 仍保留修复） |
| **代码路径** | `/home/idriver/TOOLS/auto_calibration`：`src/calibration_gui/src/tab_radar_lidar.cpp`、`src/lidar_camera_projection/src/lidar_camera_projection_node.cpp`（提交 `84cbc0c4`） |
| **完成日期** | 2026-06-12 |

---

## 修复记录

| 修改点 | 修改内容 | 提交号 | 对应根因 | 验证方式 |
|--------|----------|--------|----------|----------|
| `tab_radar_lidar.cpp onLidarSelected()` 显示标签（L332-338） | `getLidarInstall()` → `getMergedLidarParams()`，显示合并后的 LiDAR 参数（安装值 + process 标定结果） | `84cbc0c4` | 问题A：driver 未叠加已标定融合参数 | 界面标签显示合并值；点云位置回到精标位置 |
| `tab_radar_lidar.cpp onLidarSelected()` driver 启动参数（L349-361） | `getLidarInstall()` → `getMergedLidarParams()`，`driver_args` 使用 `merged.x/y/z/roll_deg/pitch_deg/yaw_deg` + `top_z_ground_offset` | `84cbc0c4` | 同上 | driver 启动参数为合并值；LiDAR 点云显示正确 |
| `lidar_camera_projection_node.cpp onTimer()` | 移除 `image_stamp` / `cloud_stamp` 时间戳同步检查及差值跳过逻辑；直接用最新图像 + 最新点云叠加，发布使用 `cloud->header` | `84cbc0c4` | 问题B：同步容差过严 + 数据源时间戳偏差大，所有帧被跳过 | 重新编译后选 `front_middle_lidar`+`front_120`，叠加图像持续更新、不再卡顿 |

---

## 修复代码 diff

### 问题A：`src/calibration_gui/src/tab_radar_lidar.cpp`（onLidarSelected）

```diff
-    double x, y, z, roll, pitch, yaw;
-    if (cfg_->getLidarInstall(current_lidar_, x, y, z, roll, pitch, yaw)) {
+    // 使用合并后的激光雷达参数（安装值 + process 标定结果）
+    ConfigManager::LidarMergedParams merged;
+    const bool has_merged = cfg_->getMergedLidarParams(current_lidar_, merged);
+    if (has_merged) {
         label_lidar_install_->setText(
             QString("x=%1m  y=%2m  z=%3m\nroll=%4°  pitch=%5°  yaw=%6°")
-            .arg(x,0,'f',3).arg(y,0,'f',3).arg(z,0,'f',3)
-            .arg(roll,0,'f',2).arg(pitch,0,'f',2).arg(yaw,0,'f',2));
+            .arg(merged.x,0,'f',3).arg(merged.y,0,'f',3).arg(merged.z,0,'f',3)
+            .arg(merged.roll_deg,0,'f',2).arg(merged.pitch_deg,0,'f',2).arg(merged.yaw_deg,0,'f',2));
     }
 
     // 启动新激光雷达 driver（如果配置了 launch_file）
     const std::string launch_path = cfg_->getLidarLaunchFilePath(current_lidar_);
     if (!launch_path.empty()) {
         ...
-        if (cfg_->getLidarInstall(current_lidar_, x, y, z, roll, pitch, yaw)) {
-            driver_args << QString("xoffset:=%1").arg(x)
-                        << QString("yoffset:=%1").arg(y)
-                        << QString("zoffset:=%1").arg(z)
-                        << QString("roll:=%1").arg(roll)
-                        << QString("pitch:=%1").arg(pitch)
-                        << QString("yaw:=%1").arg(yaw)
+        if (has_merged) {
+            driver_args << QString("xoffset:=%1").arg(merged.x)
+                        << QString("yoffset:=%1").arg(merged.y)
+                        << QString("zoffset:=%1").arg(merged.z)
+                        << QString("roll:=%1").arg(merged.roll_deg)
+                        << QString("pitch:=%1").arg(merged.pitch_deg)
+                        << QString("yaw:=%1").arg(merged.yaw_deg)
                         << QString("top_z_ground_offset:=%1").arg(cfg_->zOffset());
         }
```

### 问题B：`src/lidar_camera_projection/src/lidar_camera_projection_node.cpp`（onTimer）

```diff
     void onTimer(const ros::TimerEvent&)
     {
         cv::Mat image;
         sensor_msgs::PointCloud2::ConstPtr cloud;
-        ros::Time image_stamp;
-        ros::Time cloud_stamp;
 
         {
             std::lock_guard<std::mutex> lock(mutex_);
             if (!has_image_ || !latest_cloud_) {
                 return;
             }
             image = latest_image_.clone();
-            image_stamp = latest_image_stamp_;
             cloud = latest_cloud_;
-            cloud_stamp = latest_cloud_->header.stamp;
-        }
-
-        if (!image_stamp.isZero() && !cloud_stamp.isZero()) {
-            const double dt = std::fabs((image_stamp - cloud_stamp).toSec());
-            if (dt > sync_tolerance_sec_) {
-                ROS_WARN_THROTTLE(1.0,
-                                  "[LidarCameraProjection] image/cloud time diff too large: %.3f sec",
-                                  dt);
-                return;
-            }
         }
 
         cv::Mat overlay = renderOverlay(image, cloud);
         if (overlay.empty()) {
             return;
         }
 
-        std_msgs::Header header = cloud->header;
-        if (!image_stamp.isZero()) {
-            header.stamp = image_stamp;
-        }
-        pub_overlay_.publish(cv_bridge::CvImage(header, "bgr8", overlay).toImageMsg());
+        pub_overlay_.publish(cv_bridge::CvImage(cloud->header, "bgr8", overlay).toImageMsg());
     }
```

---

## 运行结果

### 1. 构建/打包能力（提交内新增脚本）

```
scripts/build_auto_calibration.sh           # 103 行：catkin_make 构建工作区所有 ROS 包
scripts/package_auto_calibration_runtime.sh # 427 行：打包标定软件运行环境
```

修复提交同时补齐构建与打包脚本，修复后软件可正常构建并打包发布。

### 2. 代码现状

- 当前 HEAD 仍保留修复：`tab_radar_lidar.cpp` L334 使用 `getMergedLidarParams()`（注释「使用合并后的激光雷达参数（安装值 + process 标定结果）」）；`lidar_camera_projection_node.cpp` `onTimer()` 无时间戳同步检查。
- 标定软件处于在线使用状态，修复代码随版本持续编译运行正常。

### 3. 运行验证

重新编译后启动软件、加载车辆配置、选择 `front_middle_lidar` + `front_120` 开启可视化：叠加图像持续更新不再卡顿；雷达标定界面 LiDAR 点云位于精标后正确位置。

---

## 自检

- [x] 修复代码可运行（构建脚本齐备 + 当前 HEAD 保留修复 + 软件持续使用）
- [x] 修改只针对根因（问题A 仅改参数获取两处；问题B 仅移除同步检查），不影响无关功能
- [x] 提交记录可追溯（`84cbc0c4` 单提交：源码 diff + 两份修复说明文档 + 构建脚本）

---

