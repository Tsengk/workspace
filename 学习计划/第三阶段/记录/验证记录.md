# 验证记录

> 关联输出物模板：[输出物模板.md#11.1](../输出物模板.md)
> 目标：明确验证步骤、结果和残留风险
> 关联修复提交记录：[修复提交记录.md](./修复提交记录.md)
> 对应修复提交：`84cbc0c4`（FD标定，2026-06-12）

---

## 验证基本信息

| 字段 | 内容 |
|------|------|
| **覆盖范围** | 修复结果验证：问题A 雷达标定 LiDAR 融合参数、问题B 相机-LiDAR 画面卡顿 |
| **完成标准** | 明确验证步骤、结果和残留风险 |
| **状态** | [x] 已完成 |
| **文件路径** | `./记录/验证记录.md` |
| **完成日期** | 2026-06-12 |

---

## 验证表

### 前置：重新编译

| 验证项 | 验证步骤 | 预期结果 | 实际结果 | 残留风险 |
|--------|----------|----------|----------|----------|
| 编译 | 重新编译标定软件（问题B 文档：`catkin build lidar_camera_projection`；整体可走 `scripts/build_auto_calibration.sh`） | 编译通过，无报错 | 编译通过（修复代码在后续版本持续编译使用） | 低（双 ROS 版本环境差异） |

### 问题A：雷达标定使用激光雷达融合参数

| 验证项 | 验证步骤 | 预期结果 | 实际结果 | 残留风险 |
|--------|----------|----------|----------|----------|
| 参数显示 | 启动软件 → 加载 `aian_2936` 车辆配置 → 进入雷达标定界面 → 选择某 LiDAR | 界面标签显示合并后的 LiDAR 参数（安装值 + process 标定结果），非裸安装值 | 显示合并后参数 | 低 |
| 点云位置 | 观察 LiDAR 点云在界面的位置 | 点云位于精标后的正确位置，与已标定结果一致 | 点云位置正确 | 低 |
| 保存位姿 | 手动对齐雷达点云并保存 | `radar_lidar_<radar>.yaml:calibrated:` 为正确的雷达绝对位姿，无系统性偏差 | 保存位姿正确 | 中（若其它模块仍用 `getLidarInstall()`，同类问题可能复发，需全库排查） |

### 问题B：相机-LiDAR 标定画面卡顿

| 验证项 | 验证步骤 | 预期结果 | 实际结果 | 残留风险 |
|--------|----------|----------|----------|----------|
| 叠加图像更新 | 启动软件导入车辆配置 → 「相机-LiDAR 标定」Tab → 选择 `front_middle_lidar` + `front_120` → 开启可视化 | 叠加图像持续更新，不再卡顿 | 画面持续更新，不再卡死 | 低（移除同步检查后图像与点云不再做时间对齐） |
| 对照相机回归 | 切换 `front_60` 等其它相机 | 仍流畅，行为不受影响 | 流畅，无回归 | 低 |
| 长时间运行 | 持续开启可视化观察 | 画面稳定更新，无崩溃 | 稳定 | 中（4K 高帧率场景全尺寸解码仍可能成为性能瓶颈） |

---

## 验证自检

- [x] 原始问题已复现为修复（问题A 点云位置/保存位姿、问题B 画面持续更新）
- [x] 验证结果有证据（修复提交 + 修复说明文档中的效果/验证方法）
- [x] 残留风险已说明（同类参数调用隐患、时间戳对齐、4K 解码性能）

---

## 残留风险汇总

| 风险 | 说明 | 缓解/跟踪 |
|------|------|-----------|
| 同类隐患 | 问题A：其它模块若仍调用 `getLidarInstall()`，存在同类「未合并标定参数」问题 | 全库排查 `getLidarInstall()` 调用点 |
| 时间戳对齐 | 问题B：移除同步检查后，图像与点云叠加不再保证时间对齐，对需要精确同步的场景不适用 | 当前标定场景为手动可视化，可接受；如需精确同步需另加策略 |
| 4K 性能 | 高帧率 4K 相机下全尺寸 JPEG 解码（~50ms）可能成为瓶颈 | 备选方案：`IMREAD_REDUCED_COLOR_4` 降采样（~5ms），按需实施 |
