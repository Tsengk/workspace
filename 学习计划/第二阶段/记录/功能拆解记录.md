# 功能拆解记录

> 关联输出物模板：[输出物模板.md#5.2](../输出物模板.md)
> 目标：明确实现步骤、验证点、交付结果和影响范围
> 关联任务定义记录：[任务定义记录.md](./任务定义记录.md)
> 对应版本：ap_tools 提交 `83b0f84e`（插件 `msgpack2ros4imageplugin3` 第一版）

---

## 拆解清单

| 序号 | 任务/步骤 | 涉及工具软件模块或页面 | 实现内容 | 验证点 | 交付结果 | 影响范围 |
|------|----------|----------------------|----------|--------|----------|----------|
| 1 | 插件工程搭建（目录 + CMakeLists + 双平台编译脚本） | `ap_interface_use/plugins/msgpack2ros4imageplugin3/`：`CMakeLists.txt`、`compile_1804.sh`、`compile_2004.sh` | 新建插件目录；CMake 工程 `msgpack2rosimageplugin3`（C++17），include/link idpware、ROS melodic/noetic、libde265、turbojpeg；`add_library(... SHARED cmsgpack2rosimageplugin3.cpp function4pub/avos_rospublish_jpeg_interface.cpp)`；`install` 目标到 `ap_tools_chain/runtime/x86_1804/bin`；提供 Ubuntu18.04 / 20.04 两个编译脚本 | ① 18.04 跑 `compile_1804.sh`、20.04 跑 `compile_2004.sh` 均编译通过；② 产出 `libmsgpack2rosimageplugin3.so` | 可构建插件工程 + 双平台 `.so` 产物 | 新增独立目录与安装目标 `ap_tools_chain/runtime/x86_1804/bin`；不影响既有插件 |
| 2 | 插件入口实现（CIdpAbstractPlatform + IOCTL 分发） | `cmsgpack2rosimageplugin3.cpp` | 实现 `CMsgpack2RosImagePlugin3`（open/write/read/ioctl/close）；`IOCTL_MSGPACK2ROS_PUBLISH` 将 `SPublishParam{topic, type, content}` 交给 `JpegPassthroughInterface::getInstance()->Invoke()`；导出 `CreateIdpAbstractPlatform` / `DestroyIdpAbstractPlatform` | ① 平台可动态加载插件（符号导出正常）；② 收到发布请求时进入 Invoke 调用链 | 可被 idpware 平台加载的插件入口 | 仅新增插件；与既有插件接口一致，无冲突 |
| 3 | JPEG 直通透传发布（imageType=50） | `function4pub/avos_rospublish_jpeg_interface.{h,cpp}` | `MsgImageDataListJpegPassthrough` 反序列化；遍历 images，`imageType==50` 时把 `rawData` 原样作为 JPEG 字节发布（零解码零编码）；填充 `sensor_msgs::CompressedImage`（format=jpeg、header 取 seq/timestamp/frame_id） | ① 输入 JPEG 帧 → 订阅话题收到与原字节一致的 JPEG 数据；② header 时间戳/序号正确；③ 多路图像各自独立发布 | JPEG 图像直通发布能力 | 只处理 imageType=50；不触碰其它格式 |
| 4 | H265 裸流解码 + JPEG 编码发布（imageType=53） | `function4pub/avos_rospublish_jpeg_interface.{h,cpp}`、`function4pub/h265_libde265_decoder.hpp`、`function4pub/turbojpeg_encoder.hpp` | `imageType==53` 时按 topic 懒建 `H265Libde265Decoder`（libde265 软解码，worker_threads=2，输出 I420）与 `TurboJpegEncoder`（quality=85，`tjCompressFromYUV` TJSAMP_420）；流程：`DecodeFrame` → I420 → `EncodeI420` → JPEG；等待关键帧/依赖帧属正常 | ① H265 码流输入 → 订阅到可正常打开的 JPEG 图像；② 输出宽高与码流一致；③ 首帧需 IDR，等待期间不报错不崩溃 | H265 裸流转 JPEG 发布能力 | 仅 imageType=53；decoder/encoder 按 topic 独立实例，多相机 topic 互不影响；编码质量固定 85 |
| 5 | ROS 话题映射发布与不支持格式处理 | `function4pub/avos_rospublish_jpeg_interface.cpp` | 话题命名 `topic_name/索引/compressed`（plugin1 形式）；`rospub_map` 缓存 Publisher（queue=1）；`imageType` 非 50/53（如 H264=52、裸格式 0-19）用 `logged_types` 记录一次后跳过，防刷屏 | ① N 路图像 → N 个独立话题（0/1/2…）；② 不支持格式只提示一次、不发布、不崩溃 | 正确的话题映射 + 非目标格式优雅跳过 | 话题命名沿用 plugin1 约定；与 plugin2 的 frameID 话题命名不同（互不影响） |
| 6 | 整体编译、加载与联调自测 | 以上全部 + `ap_tools_chain` 运行环境 | 双平台编译产出 `.so` 并部署到运行目录；在平台上加载插件，用真实 `MsgImageDataList` 数据（JPEG + H265 混合）联调验证两条发布链路 | ① 平台加载成功；② JPEG 直通与 H265→JPEG 两条链路实测通过；③ 长时间运行无崩溃、无明显内存泄漏；④ 代码留痕提交 `83b0f84e` | 可运行的插件第一版 + 自测结果留痕 | 影响部署环境 `ap_tools_chain/runtime/x86_1804/bin`；不影响其它插件 |

---

## 拆解检查

- [ ] 每个步骤都有明确验证点
- [ ] 每个步骤的交付结果可验收
- [ ] 已标明影响范围（不会波及无关功能）
- [ ] 步骤之间依赖关系清楚（1→2→3/4→5→6 顺序执行，3 与 4 相互独立可并行验证）

---

