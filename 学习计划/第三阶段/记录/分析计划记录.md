# 分析计划记录

> 关联输出物模板：[输出物模板.md#9.2](../输出物模板.md)
> 目标：明确分析路径、验证方式和相关模块/页面/脚本
> 关联问题定义记录：[问题定义记录.md](./问题定义记录.md)（问题A 雷达标定 LiDAR 融合参数 / 问题B 相机-LiDAR 画面卡顿）
> 对应修复提交：`84cbc0c4`（FD标定，2026-06-12）

---

## 分析计划基本信息

| 字段 | 内容 |
|------|------|
| **覆盖范围** | 两个问题的分析路径、定位入口、验证方式 |
| **结合场景** | 问题定义记录 9.1（auto_calibration：雷达-LiDAR 标定、相机-LiDAR 标定） |
| **完成标准** | 明确分析路径、验证方式和相关模块/页面/脚本 |
| **状态** | [x] 已完成 |
| **文件路径** | `./记录/分析计划记录.md` |
| **完成日期** | 2026-08-21 |

---

## 分析计划表

### 问题A：雷达标定未使用激光雷达融合参数

| 序号 | 定位入口 | 分析步骤 | 验证方式 | 涉及模块/页面/脚本 | 时间安排 |
|------|----------|----------|----------|--------------------|----------|
| A1 | 雷达标定 Tab 选择 LiDAR 时 driver 启动参数 → `src/calibration_gui/src/tab_radar_lidar.cpp:onLidarSelected()`（L288） | ① 复现：加载 `aian_2936` 配置进入雷达标定界面，选择某 LiDAR，对比点云位置与精标结果；② 阅读 `onLidarSelected()` 确认 LiDAR 参数来源（`getLidarInstall()` 裸安装值 还是 `getMergedLidarParams()` 合并值）；③ 检查 `lidarIsCalibrated()` 前置校验（L58）与参数传入 driver 的链路（L305、L334） | 打印合并前后参数（裸安装值 vs 合并值）对比；标定界面观察点云位置是否回到精标位置；保存 `radar_lidar_<radar>.yaml:calibrated` 校验位姿 | `tab_radar_lidar.cpp`、`ConfigManager::getLidarInstall()/getMergedLidarParams()/lidarIsCalibrated()`、`aian_2936.yaml`、`process/` 标定结果 | 半天 |
| A2 | 参数合并实现 → `ConfigManager::getMergedLidarParams()` | ① 确认合并优先级与字段：基础 `aian_2936.yaml` 安装值 + 覆盖 `lidar_rp_ → secondary_rp_ → lidar_yaw_ → lidar_lidar_`；② 确认存在至少一个 process 文件时返回值有效（对齐 `lidarIsCalibrated()` 语义） | 对已标定车辆日志输出合并值，核对叠加顺序与数值 | `ConfigManager` 合并逻辑、`process/` 标定结果文件 | 与 A1 并行，半天 |

### 问题B：相机-LiDAR 标定画面卡顿

| 序号 | 定位入口 | 分析步骤 | 验证方式 | 涉及模块/页面/脚本 | 时间安排 |
|------|----------|----------|----------|--------------------|----------|
| B1 | 投影节点日志 `[LidarCameraProjection] image/cloud time diff too large: 6.700 sec` → `src/lidar_camera_projection/src/lidar_camera_projection_node.cpp:onTimer()`（L331） | ① 复现：选 `front_middle_lidar` + `front_120`，叠加图像不更新；② 阅读 `onTimer()` 内时间戳同步检查（`image_stamp`/`cloud_stamp` 差值 > `sync_tolerance_sec_` 则 return 跳过整帧）；③ 对比 `front_60` 等流畅相机的 bag 时间戳与 `front_middle_lidar` 的差值；④ 确认 `front_120` 相机与 `front_middle_lidar` 数据源不同导致时间戳相差 6~7s | 打印 image/cloud 时间戳差值，确认所有帧均被同步检查跳过（画面不更新的根因证据）；切 `front_60` 验证对照 | `lidar_camera_projection_node.cpp:onTimer()`、`sync_tolerance_sec_`（默认 0.10，L551，launch 可配 L109）、`front_120`/`front_middle_lidar` bag 数据源 | 半天 |

---

## 计划检查

- [x] 每个入口都有明确的分析步骤（A1/A2/B1/B2 均含复现 → 阅读 → 验证闭环）
- [x] 每步都有验证方式（日志打印 / 界面观察 / 文件校验 / 耗时实测）
- [x] 涉及模块/页面/脚本已列清（源码文件 + 函数 + 配置 + 数据源）
- [x] 有合理的时间安排（按半天粒度，A1/A2 可并行，B2 为可选）

---

